Read the skill file at skills/learning/062_fast_learning_plan.md and execute the Core Prompt using the parameter values provided below. Substitute each value into the corresponding {placeholder}. For any parameter not supplied, use the default specified in the skill file.

Use web search to find real, currently available resources. Every resource recommendation must be verifiable — do not fabricate titles, authors, or URLs.

Inputs: $ARGUMENTS